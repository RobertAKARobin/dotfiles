desk="$HOME/Desktop"
w="$HOME/Programming/web"
games='/usr/share/emacs/22.1/lisp/play/'
droplet="192.241.177.50"
h="$w/lib/helperjs"
pantemp="/usr/local/Cellar/pandoc/1.17.0.3/share/x86_64-osx-ghc-7.10.3/pandoc-1.17.0.3/data/templates"

alias iscripts='cd /Applications/Adobe\ Illustrator\ CS6/Presets.localized/en_US/Scripts'
alias gaev='cp $w/ga/evaluation.md . && vim evaluation.md'

source ~/.gh_access

function hc(){
  cp "$h/helpers.js" helpers.js
}

function html(){
  cp -pR $w/lib/default_html $1
  cd $1
  git init
  chrome index.html
  vim index.html
}

function letter(){
  FILENAME="$(date +%y%m%d)_$1.html"
  cp $w/lib/letter.html "$w/letters/$FILENAME"
  cd $w/letters
  chrome $FILENAME
  vim $FILENAME
}

function nougat(){
  # Because it's like "new git"
  # $1 is "ga" or "me
  # $2 is name
  # $3 is description

  url="https://api.github.com/"
  if [ "$1" == "ga" ]; then
    url+="orgs/ga-wdi-curriculum"
    acct="ga-wdi-curriculum"
  elif [ "$1" == "me" ]; then
    url+="user"
    acct="robertakarobin"
  else
    echo "Indicate 'me' or 'ga', dummy."
    return
  fi
  url+="/repos"
  params='{"name":"'$2'", "description":"'[$3]'"}'
  curl -vvvv "$url" --include --user robertakarobin:"$GH_ACCESS" -A "$useragent" -d "$params"
  git init
  git remote add origin "git@github.com:$acct/$2.git"
  git add .
  git commit -m "Initial commit"
}

export PATH="/Users/robertthomas/.rvm/gems/ruby-2.3.1/bin:/Users/robertthomas/.rvm/bin:/Users/robertthomas/.rvm/gems/ruby-2.3.1@global/bin:/Users/robertthomas/.rvm/rubies/ruby-2.3.1/bin:/usr/local/heroku/bin:/usr/local/bin:/usr/local/sbin:/usr/bin:/bin:/usr/sbin:/sbin"

